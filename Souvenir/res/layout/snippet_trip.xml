<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:padding="5dp"
    android:background="@android:color/white"
    android:orientation="vertical" >

    <!-- <ImageView
        android:id="@+id/trip_snippetPic"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentLeft="true"
        android:scaleType="centerCrop" />
	-->
    <TextView
        android:id="@+id/trip_snippetName"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:lines="1"
        android:scrollHorizontally="true"
        android:textColor="@color/primaryRed"
        android:textSize="25sp"
        android:textStyle="bold"
        android:typeface="sans" 
        />

    <!-- <TextView
        android:id="@+id/trip_snippetGeneralLocation"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:layout_weight="1"
        android:lines="2"
        android:scrollHorizontally="true"
        android:textAlignment="gravity"
        android:textColor="@android:color/darker_gray"
        android:layout_below="@id/snippetEvent"
        android:layout_toRightOf="@id/trip_snippetPic" />

    <TextView
        android:id="@+id/trip_snippetStartDate"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:ellipsize="end"
        android:lines="1"
        android:scrollHorizontally="true"
        android:textColor="@android:color/black" 
        android:layout_below="@id/snippetLocation"
        android:layout_toRightOf="@id/trip_snippetPic"/>

    <TextView
        android:id="@+id/trip_snippetEndDate"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:ellipsize="end"
        android:lines="1"
        android:scrollHorizontally="true"
        android:textColor="@android:color/black" 
        android:layout_below="@id/snippetText"
        android:layout_toRightOf="@id/trip_snippetPic"/> -->

</RelativeLayout>